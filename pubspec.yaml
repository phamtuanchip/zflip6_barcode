name: zflip6_barcode
description: "ZFlip6 Barcode - Scan and store barcodes, display on Samsung Z Flip 6 cover screen."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^5.2.3
  sqflite: ^2.3.3+1
  path: ^1.9.0
  qr_flutter: ^4.1.0
  barcode_widget: ^2.0.4
  provider: ^6.1.2
  intl: ^0.19.0
  share_plus: ^10.1.3
  flutter_slidable: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
